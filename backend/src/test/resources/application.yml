spring:
  security:
    oauth2:
      client:
        registration:
          spotify:
            client-id: ${SPOTIFY_CLIENT_ID:test-id}
            client-secret: ${SPOTIFY_SECRET_ID:test-secret}
            scope: scope1,scope2
